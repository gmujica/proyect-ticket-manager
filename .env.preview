# El build de la rama dev, el que sale publicado en el link de preview. Apunta al
# Worker de staging y no al de producción: lleva las mismas migraciones, pero
# escribe en ptm-board-dev, así que probar en el preview no toca datos reales.
#
# Igual que en producción, tiene que coincidir exactamente con el FRONTEND_ORIGIN
# que ptm-api-dev tiene configurado, sin barra final.
VITE_API_URL=https://ptm-api-dev.gmujica.workers.dev
