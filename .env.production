# El Worker desplegado. Tiene que coincidir exactamente con el origen que la API
# tiene configurado en FRONTEND_ORIGIN, en los dos sentidos y sin barra final.
VITE_API_URL=https://ptm-api.gmujica.workers.dev
